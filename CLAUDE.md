@AGENTS.md

The Logix Designer SDK is Windows + Python 3.12 only and never bundled — if
`logix_designer_sdk not found` appears, point the operator at
`scripts/_sdk_paths.py` resolution options (LOGIX_DESIGNER_SDK_WHEEL /
LOGIX_DESIGNER_SDK_SITE / licensed Studio 5000 install).